import { NoDoubleTapDirective } from './no-double-tap.directive';

describe('NoDoubleTapDirective', () => {
  it('should create an instance', () => {
    const directive = new NoDoubleTapDirective();
    expect(directive).toBeTruthy();
  });
});
