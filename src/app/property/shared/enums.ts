export enum Category {
  Lettings,
  LongTerm,
  ShortTerm,
  Sales
}

export enum LetTerm {
  Short,
  Long
}

export enum StatusType {
  Valuations,
  Instructions,
  Viewings,
  Offers
}

export enum PropertySaleType {
  Sales,
  Lettings,
  All,
  None
}

export enum Managed {
  Yes,
  No,
  Either
}
