import { AddHeader.Interceptor } from './add-header.interceptor';

describe('AddHeader.Interceptor', () => {
  it('should create an instance', () => {
    expect(new AddHeader.Interceptor()).toBeTruthy();
  });
});
