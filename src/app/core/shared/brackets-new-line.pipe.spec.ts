import { BracketsNewLinePipe } from './brackets-new-line.pipe';

describe('BracketsNewLinePipe', () => {
  it('create an instance', () => {
    const pipe = new BracketsNewLinePipe();
    expect(pipe).toBeTruthy();
  });
});
