import { CustomReuseStrategy } from './custom-reuse-strategy';

describe('CustomReuseStrategy', () => {
  it('should create an instance', () => {
    expect(new CustomReuseStrategy()).toBeTruthy();
  });
});
